# SRB2Kart bundles ~380 MB of WADs in assets/. AGP's compressAssets task buffers
# large files in memory, so the default 512 MB heap OOMs on textures.kart (176 MB).
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m
android.useAndroidX=false
